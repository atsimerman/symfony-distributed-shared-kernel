{
    "name": "atsimerman/symfony-distributed-shared-kernel",
    "description": "Shared kernel: aggregate identities, universal value types, marker interfaces, and event contracts. No framework dependencies, no domain logic, no statuses.",
    "type": "library",
    "license": "proprietary",
    "require": {
        "php": ">=8.4"
    },
    "autoload": {
        "psr-4": {
            "SharedKernel\\": "src/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
